pipeline {
   agent any
     
   stages {
      stage('Check Python Installed or not -- Pre-requestic') {
         steps {
            bat 'echo "Check Python Version"'
            bat 'python --version'
                           
            }
         }
      }
   }
